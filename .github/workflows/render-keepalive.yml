name: Render Keep Alive

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health
        run: curl -sf --max-time 30 "${API_HEALTH_URL:-https://bomberman-api-s4ix.onrender.com/health}"
        env:
          API_HEALTH_URL: ${{ secrets.API_HEALTH_URL }}
