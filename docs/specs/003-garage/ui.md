# UI

## Telas (wireframe.png)

- **09 Meus carros** — header com avatar e KPIs (não duplica perfil; lista vertical com cards de carro mostrando cover, nickname, brand+model, badge Stage e KPIs HP/peso/peso-pot).
- Estados: empty (com CTA), loading (skeleton card), error.

## Eventos

- Tap em um CarCard → `/cars/:id` (M8).
